# Copy to .env.local and fill in — never commit real values here.
#
# The live project (confirmed 2026-08-24 against backend/.env in the
# tripagent-full checkout, and by directly querying its REST API): the
# consumer site_* tables (site_members, site_saved_items, site_member_events,
# site_member_prefs) live on project ref gnifmusartvwngcuquou. This is
# DIFFERENT from the older ref (owqmlofqgvyqkgazayoy) still hardcoded as
# js/backend.js's fallback default and referenced in SETUP.md/001_site_consumer_funnel.sql's
# header comment — those predate the "My Year" (site_saved_items) work and
# point at the wrong project for this schema. Do not copy that value here.
VITE_SUPABASE_URL=https://gnifmusartvwngcuquou.supabase.co

# The anon (publishable) key — safe for the browser, RLS enforces security.
# Get the current one from Supabase Studio -> Project Settings -> API, or
# from tripagent-full/backend/.env's SUPABASE_ANON_KEY (same project).
VITE_SUPABASE_ANON_KEY=

# Google Maps JavaScript API key, used by src/components/CityMap.tsx (the
# city-guide points-of-interest map, Stays/Tables/Sights/After dark toggle).
# Must have the Maps JavaScript API enabled and be restricted to this site's
# origin(s) in Google Cloud Console. Without it the map tab shows a fallback
# message instead of a blank box.
VITE_GOOGLE_MAPS_API_KEY=

# WhatsApp Business number (digits only, country code, no +) for the "Speak
# with your advisor" links (src/components/layout/Footer.tsx and elsewhere).
# Mirrors js/account.js's handoff() fallback: unset here, these links point
# at /enquire instead of a dead wa.me/REPLACE_NUMBER placeholder.
VITE_WHATSAPP_NUMBER=

# The real, deployed Aanya concierge backend (backend/app/routers/ai_router.py,
# on Render) — used by src/concierge-chat/api.ts (ported from the standalone
# concierge-chat/ app, reused as-is by src/pages/ConciergePage.tsx). Public
# endpoint URL, not a secret, so — like VITE_SUPABASE_URL above — the real
# value is filled in here rather than left blank.
VITE_ASSISTANT_ENDPOINT=https://tripagent-customersite-be.onrender.com/ai/concierge/chat

# Base URL of the same FastAPI backend (backend/main.py) — used by
# src/pages/EnquirePage.tsx's POST /enquiries (Phase A). Left blank in
# production: the frontend then fetches a same-origin relative path,
# matching concierge-chat/src/api.ts's PROD_DEFAULT_ENDPOINT convention.
# Set for local dev, where the backend runs on its own port (e.g.
# http://localhost:8000).
VITE_API_BASE_URL=
